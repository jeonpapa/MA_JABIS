# 자격증명 (절대 커밋 금지)
config/foreign_credentials.json
config/.env
# 로그인 세션 쿠키 (Micromedex 등 — 절대 커밋 금지)
data/foreign/*/storage_state.json

# 프론트엔드 빌드 산출물·의존성
frontend/node_modules/
frontend/out/
frontend/dist/

# 작업용 임시 디렉터리
.m1tmp/

# DB 파일
data/db/*.db
data/db/*.db-shm
data/db/*.db-wal
data/db/*.db.bak*
data/db/*.db.bak.*
# 루트에 실수로 생성되는 빈 stub DB 차단 (실제 DB 는 data/db 만 사용)
/drug_prices.db
/drug_prices.db-shm
/drug_prices.db-wal

# 캐시 (대용량, 별도 백업)
data/hta_cache/

# 사용자 업로드 PDF (영구 보존이지만 git 부적합 — 별도 백업 필요)
data/uploads/

# 멀티에이전트 컨텍스트 (Claude Code memory/plans symlinks — 개인정보 포함, git 추적 차단)
.agent-context/

# 로그 (전체 — server.out/scheduler.out 포함)
logs/

# Python
__pycache__/
*.pyc
*.pyo
.DS_Store

# 가상환경
.venv/
venv/

# Claude Code 세션 메타
.claude/
.gstack/
